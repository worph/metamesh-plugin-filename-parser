node_modules/
dist/
# Locally-built @metazla/filename-tools tarball (vendored file: dep; rebuilt from
# packages/filename-tool — see Dockerfile note). Build artifact, not source.
vendor/
*.log
.env
.DS_Store
.pnpm-store
